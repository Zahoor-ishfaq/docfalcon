server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Don't cache the entry point — always fetch fresh
    location = /index.html {
        add_header Cache-Control "no-store";
    }
}